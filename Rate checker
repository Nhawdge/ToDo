function checkNumbers(){
var dl= document.querySelectorAll('#download')

var ul = document.querySelectorAll('#download')

var jitter = document.querySelectorAll('#download')

var latency = document.querySelectorAll('#download')

return dl > 100 && ul > 10 && jitter < 500 && latency < 50;
}
